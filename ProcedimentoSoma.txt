algoritmo "semnome"
var
  X, Y:Inteiro
Procedimento Soma(A,B: Inteiro)
Inicio
      Escreval("Recebi o valor",A)
      Escreval("Recebi o valor",B)
      Escreval("A soma entre os dois e",A+B)



FimProcedimento
inicio
      X <- 5
      Y <- 3
      Soma(X,Y)

fimalgoritmo

 // Ele é apenas copiado pra dentro do parametro e não afetara a variavel original (X e Y) 
 // O Valor de X e Y não é alterado, diferente da Soma (var A, B: Inteiro) = Referencia